require 'spec_helper'

describe Lita::Weather do
  it 'has a version number' do
    expect(Lita::Weather::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
